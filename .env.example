# AURORA Environment Configuration
# Copy this file to .env and customize for your environment

# ============================================================================
# Database Configuration (Required for persistent learning)
# ============================================================================

# Database URL - SQLite for development, PostgreSQL for production
#
# SQLite (Development - No installation required):
#   - Relative path: sqlite:///./aurora.db (creates file in project root)
#   - Absolute path: sqlite:////absolute/path/to/aurora.db
#   - In-memory (data lost on restart): sqlite:///:memory:
#
# PostgreSQL (Production):
#   - Format: postgresql://username:password@host:port/database
#   - Example: postgresql://aurora_user:secretpass@localhost:5432/aurora_db
#
DATABASE_URL=sqlite:///./aurora.db

# ============================================================================
# Security Configuration (Required)
# ============================================================================

# JWT Secret Key - MUST change in production! Generate with: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-jwt-key-change-in-production-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Allowed Origins - Comma-separated list of allowed origins
# IMPORTANT: Update this for production with your actual frontend URL
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
